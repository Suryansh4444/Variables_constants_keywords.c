#include <stdio.h>  // Include the standard input/output library

int main() {
    /* 
     * This is a multi-line comment.
     * It can span multiple lines.
     * The C compiler ignores everything between the opening /* and the closing * /.
     * You can use this to provide detailed explanations or temporarily disable blocks of code.
     */

    // Declare an integer variable
    int number = 42;

    /* 
     * Print the value of the integer variable.
     * The %d format specifier is used to print integers.
     */
    printf("The value of the number is: %d\n", number);

    /* 
     * Another example of a multi-line comment.
     * This can be useful for commenting out larger sections of code or
     * providing a more detailed description of what the code does.
     */
    
    // Return statement to indicate successful execution
    return 0;
}
